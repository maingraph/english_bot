[build]
  command = "echo 'No build needed'"
  publish = "public"

[build.environment]
  PYTHON_VERSION = "3.11"

# API routes to Netlify Function
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/tma-api"
  status = 200

# All other routes to index.html (SPA)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

